#!/usr/bin/env bash

args=""
for arg in "$@"; do
  args="$args $arg"
done

docker exec -ti -u dev scrum_master_php sh \
    -c "cd /srv/edi-to-json && vendor/bin/phpunit $args --coverage-html coverage"
